import React from 'react';

// import { Container } from './styles';

function Header({ children }) {
  return (
    <>
      <h1> header </h1>
      {children}
    </>
  );
}

export default Header;
